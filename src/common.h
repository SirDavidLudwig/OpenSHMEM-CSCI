#pragma once

/**
 * The maximum hostname length (including null terminator)
 */
#define HOSTNAME_LEN 256

/**
 * Used to identify my shared memory objects on the shared cluster
 */
#define SHARED_MEMORY_PREFIX "dwl2x"

/**
 * The starting port number to use
 */
#define PORT_BASE 36500
